name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up secret file
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DISTRIBUTION_ID: ${{ secrets.AWS_DISTRIBUTION_ID }}
          AWS_S3_BUCKET_NAME: ${{ secrets.AWS_S3_BUCKET_NAME }}
        run: |
          echo ${{secrets.AWS_ACCESS_KEY_ID}} | sed 's/./& /g' >> secrets.txt
          echo ${{secrets.AWS_SECRET_ACCESS_KEY}} | sed 's/./& /g' >> secrets.txt
          echo ${{secrets.AWS_DISTRIBUTION_ID}} | sed 's/./& /g' >> secrets.txt
          echo ${{secrets.AWS_S3_BUCKET_NAME}} | sed 's/./& /g' >> secrets.txt
